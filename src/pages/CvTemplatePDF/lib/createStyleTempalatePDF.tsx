import { StyleSheet } from '@react-pdf/renderer';

import { StyleOptionType } from '../const';

const style = StyleSheet.create({});

export const createStyleTempalatePDF = (dataStyle: StyleOptionType) => {
  console.log(dataStyle, style);
};
